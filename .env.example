# Copy to ".env.local" (Next.js) and adjust. Public vars must be prefixed with NEXT_PUBLIC_.

# Backend base URL (Spring Boot default port)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Must match backend app.token-estimation.fixed-output-tokens (default 100)
NEXT_PUBLIC_FIXED_OUTPUT_TOKENS=100
